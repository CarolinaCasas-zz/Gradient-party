/* 
import { useState } from "react";
import { RiSunFill, RiMoonFill, RiTrafficLightLine } from "react-icons/ri";
import {ThemeProvider} from "styled-components"

    const ligthTeme ={
        pageBackground:"white",
        titleColor:"#dc658b",
        tagLineColor:"black"
    }

    
    const darkTheme ={
        pageBackground:"#282c36",
        titleColor:"#dc658b",
        tagLineColor:"black"
    }

    const themes ={
        light:ligthTeme,
        dark: darkTheme
    }

    const DarkMode=()=>{
        const[theme, setTheme]=useState("light")
}

export default DarkMode */